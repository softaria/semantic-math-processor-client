/**
 * @hidden
 * @packageDocumentation
 */
import { MathStructure } from "../model";
import { UndoRedoEditor } from "../../../standard-edition/editor/undo-redo-editor";
import { AnyNode } from "../../../standard-edition/model/model-base";
export interface Heuristic {
    apply(tree: MathStructure, editor: UndoRedoEditor): boolean;
}
export declare namespace Heuristics {
    function getDefault(): Heuristic[];
    function getAll(): Heuristic[];
    class VisibleMultiplyHeuristic implements Heuristic {
        apply(tree: MathStructure, editor: UndoRedoEditor): boolean;
        isEndWithValue(node: AnyNode): boolean;
    }
}
