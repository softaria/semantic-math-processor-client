/**
 * @hidden
 * @packageDocumentation
 */
import { VisualSymbol } from "../structure/visual-levels";
import { SelectionVisitor } from "../engine/selection/utils";
import { EmptySelection } from "../engine/selection/empty-selection";
import { LevelSelection } from "../engine/selection/level-selection";
import { SpaceSelection } from "../engine/selection/space-selection";
export declare class SelectionSymbolsCollector implements SelectionVisitor<VisualSymbol[]> {
    static readonly instance: SelectionSymbolsCollector;
    visitEmpty(selection: EmptySelection): VisualSymbol[];
    visitLevel(selection: LevelSelection): VisualSymbol[];
    visitSpace(selection: SpaceSelection): VisualSymbol[];
}
