/**
 * @hidden
 * @packageDocumentation
 */
import { AbstractNode, ParseValue } from "./AST";
import { KatexNodeVisitor } from "./visitors";
export declare class Supsub extends AbstractNode {
    base: AbstractNode[];
    sup: AbstractNode[];
    sub: AbstractNode[];
    widthSources: AbstractNode[];
    heightSources: AbstractNode[];
    static command: string;
    constructor(base: AbstractNode[], sup: AbstractNode[], sub: AbstractNode[], widthSources?: AbstractNode[], heightSources?: AbstractNode[]);
    toParseValue(): ParseValue;
    accept<T>(visitor: KatexNodeVisitor<T>): T;
}
