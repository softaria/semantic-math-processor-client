/**
 * @hidden
 * @packageDocumentation
 */
import { AbstractNode, ParseValue, Size } from "./AST";
import { KatexNodeVisitor } from "./visitors";
export declare class Kern extends AbstractNode {
    dimension: Size;
    constructor(dimension: Size);
    toParseValue(): ParseValue;
    accept<T>(visitor: KatexNodeVisitor<T>): T;
}
