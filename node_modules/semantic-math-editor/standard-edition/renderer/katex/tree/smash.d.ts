/**
 * @hidden
 * @packageDocumentation
 */
import { AbstractNode, ParseValue } from "./AST";
import { KatexNodeVisitor } from "./visitors";
export declare class Smash extends AbstractNode {
    body: AbstractNode[];
    argT: boolean;
    argB: boolean;
    static command: string;
    constructor(body?: AbstractNode[], argT?: boolean, argB?: boolean);
    toParseValue(): ParseValue;
    accept<T>(visitor: KatexNodeVisitor<T>): T;
}
