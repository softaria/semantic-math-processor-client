/**
 * @hidden
 * @packageDocumentation
 */
import { Terminal } from "../model/model-base";
export declare class TerminalSerializer {
    private readonly indexedNodes;
    constructor(indexedNodes: Terminal[]);
    serialize(terminal: Terminal): string;
    deserialize(serialized: string): Terminal;
}
