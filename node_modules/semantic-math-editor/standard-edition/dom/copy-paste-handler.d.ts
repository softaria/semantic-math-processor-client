/**
 * @hidden
 * @packageDocumentation
 */
import { SelectionEditor } from "../editor/selection-editor";
export interface CopyPasteCallbacks {
    onCopy(source: string): void;
    onCut(source: string, hadSelection: boolean): void;
    onPaste(source: string, hadSelection: boolean): void;
}
export interface CopyPastePermissions {
    canCopy(): boolean;
    canCut(): boolean;
    canPaste(): boolean;
}
export declare class CopyPasteHandler {
    private readonly editor;
    private readonly permissions;
    private readonly callbacks;
    private id;
    readonly destroy: () => void;
    constructor(editor: SelectionEditor, permissions: CopyPastePermissions, callbacks: CopyPasteCallbacks);
    private onCopy;
    private onCut;
    private onPaste;
    private setDataToBuffer;
    private getDataFromBuffer;
}
