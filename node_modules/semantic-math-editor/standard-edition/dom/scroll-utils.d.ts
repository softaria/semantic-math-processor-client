/**
 * @hidden
 * @packageDocumentation
 */
import { SelectionEditor } from "../editor/selection-editor";
import { DomRenderedTree } from "../editor/editor-support";
export declare class ScrollUtils {
    private readonly editor;
    private readonly element;
    private readonly getRenderedTree;
    constructor(editor: SelectionEditor, element: HTMLElement, getRenderedTree: () => DomRenderedTree);
    scrollIntoView(): void;
    private scrollElementIntoView;
}
