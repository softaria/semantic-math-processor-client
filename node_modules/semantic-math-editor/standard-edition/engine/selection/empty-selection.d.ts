/**
 * @hidden
 * @packageDocumentation
 */
import { InnerSelection, SelectionHistory, SelectionVisitor } from "./utils";
import { Terminal, TerminalPointer } from "../../model/model-base";
export declare class EmptySelection implements InnerSelection {
    pointer: TerminalPointer;
    selectionHistoryManager: SelectionHistory;
    constructor(pointer: TerminalPointer, selectionHistoryManager: SelectionHistory);
    get size(): number;
    get terminals(): Terminal[];
    accept<T>(visitor: SelectionVisitor<T>): T;
}
