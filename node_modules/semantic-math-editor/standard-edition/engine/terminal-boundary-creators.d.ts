/**
 * @hidden
 * @packageDocumentation
 */
import { AnyNode, Terminal, TerminalPointer } from "../model/model-base";
export declare class TerminalPointerBoundaryCreator {
    static createPointer(node: AnyNode, left: boolean, recursive?: boolean): TerminalPointer;
}
export declare class TerminalBoundaryCreator {
    private static leftInstance;
    private static rightInstance;
    private left;
    private constructor();
    static getBoundary(node: AnyNode, left: boolean, recursive?: boolean): Terminal;
    create(node: AnyNode, recursive?: boolean): Terminal;
}
