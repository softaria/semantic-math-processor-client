/**
 * @hidden
 * @packageDocumentation
 */
import { Terminal } from "./model-base";
export declare enum ErrorSeverity {
    emptyPlaceholder = "emptyPlaceholder",
    insuffientExpression = "insuffientExpression",
    incorrectExpression = "incorrectExpression",
    expressionAtWrongPlace = "expressionAtWrongPlace"
}
export declare class SemanticError {
    readonly terminals: number[];
    readonly description: string;
    readonly severity: ErrorSeverity;
    readonly allowDeeperValidation: boolean;
    constructor(terminal: number | number[], desc: string, severity: ErrorSeverity, allowDeeperValidation?: boolean);
    includes(terminal: Terminal): boolean;
}
