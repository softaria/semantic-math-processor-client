/**
 * @hidden
 * @packageDocumentation
 */
import { AnyNode, TerminalPointer } from "./model-base";
export declare function isNonTypableTextSymbol(char: string): boolean;
export declare class InsertOperation {
    pointer: TerminalPointer;
    node: AnyNode;
    prohibitElementReplacement: boolean;
    constructor(n: AnyNode, p: TerminalPointer);
    toString(): string;
    static fromChar(char: string, isPlain: boolean): InsertOperation;
    static fromKey(key: number, isPlain?: boolean): InsertOperation;
}
